# TODO-105. README からスライド作成・公開までの流れを分かるようにする

|      | main | 担当 |
|------|------|------|
| 見込み | GPT-6 / reasoning effort は着手時に確認 | main（修正）+ reviewer + verifier |
| 実施 | GPT-6 Astra / reasoning effort medium | main（修正）+ reviewer + verifier |

| 担当 | モデル | reasoning effort |
|------|--------|------------------|
| main | GPT-6 Astra | medium |
| reviewer | GPT-5.6 Sol | high |
| verifier | GPT-5.6 Luna | medium |

main は Codex の設定、各担当は Codex の担当定義を使用した。
token・料金の測定手段は無いため推測せず、Claude Code の集計は流用していない。

## きっかけ

README を読むスライド作成者にとって、インストール不要の範囲、ファイルの入手、
最初の 1 枚の保存、AI への依頼、端末ごとの音声が分かりにくかった。
項目登録後、利用者から実行を依頼された。
公開の詳細は不要という指定に従い、公開先の用意やサービス別の操作説明は見送った。

## やったこと

- README に準備・作成・確認・公開・URL 共有へのリンクを追加した。
- 再生とコピー・編集には Python・uv・CLI が不要であることを示し、
  CLI を使う操作を分けて説明した。
- ZIP の入手先とコピー方式を追加し、断片例から完全な最小例へ案内した。
  コピー後に CLI を使うときは `init` で一覧のファイルも用意する手順にした。
- User.md に AI へ渡す資料、依頼文、生成結果の保存先と確認方法を追加した。
- README からネット接続、端末ごとの声、音声再試行・切り替えへ案内した。
- readme・user スライドの対応する説明を同期した。

## テスト

- `uv run pytest`: 26 passed。
- reviewer が文書・スライド・実装を照合し、指摘反映後に問題が無いことを確認した。
- verifier が空の作業場所でコピー方式・CLI 方式・最小例の保存・一覧生成を再現した。
- README/User.md の相対リンクと見出しリンクは全件解決。ZIP と raw Markdown は HTTP 200。
- Playwright の headless shell で 1280×720・390×844 の最小例と変更対象スライドを確認。
  本文枠からのはみ出しは 0 件、前後送りも成功した。
- 最小例の Online Voice は音声長 2.784 秒、再生位置の進行と朗読・再生完了を確認。
  聴取はしていない。通常 Chromium の起動制約と TTS 送信の自動審査拒否に対し、
  表示時は外部 TTS を遮断し、音声確認は公開済みの最小例一文だけに限定して承認された。
- `git diff --check`: 通過。

## 分担の振り返り

- reviewer はコピー方式から CLI へ進む際の `index.html` 不足と、
  CLI 方式で AI に渡す User.md の入手方法の不足を見つけた。どちらも修正した。
- verifier はコピー後の CLI 導入、リンク、PC・スマホ幅の表示と送り、最小例の音声再生を
  確認した。ブラウザ環境と自動審査の制約があり、main が起動方法と通信範囲を切り分け、
  verifier がその条件で独立に再確認した。
- 担当の分け方は見込みどおり。次回も文書間の照合を先に行い、修正後に手順を再現する。
  ブラウザ起動と通信条件を先に短い確認で固め、再現担当へまとめて渡す。

分担理由・依頼・各報告は [担当の記録](../agents/TODO-105/README.md) にある。
