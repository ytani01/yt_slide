# TODO-111. 文書とソースコメントの不自然な日本語を直す

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5 / effort high | verifier |
| 実施 | Opus 5 / effort high | verifier |

| 担当 | モデル | effort | output | cache_creation | 料金の割合 |
|------|--------|--------|--------|----------------|-----------|
| main | Opus 5 | high | 20,075 | 54,857 | 80% |
| verifier | Sonnet 5 | medium | 14,698 | 104,458 | 20% |
| 合計 |  |  | 34,773 | 159,315 | 概算 $4.9 |

- verifier は定義（`~/.claude/agents/verifier.md`）のまま Sonnet 5 /
  effort medium。表示の確認とコマンドの実行だけなので上書きしていない
- 集計は立てたコミットからのもので、その前に行った調査（他に変な日本語が
  無いかの洗い出し）の分は入っていない

## きっかけ

TODO-110 で `docs/User.md` の「直す枚」を「直すスライド」に直したあと、
利用者から「他にも変な日本語がないか再確認して。ソース内のコメントも」と
指示があった。文書・スライド・`player.html`・Python のコメントと docstring を
ひととおり読み、同種のものと、文法・語法・表記ゆれの問題を洗い出した。

## やったこと

19 ファイル、151 行。挙動は変えていない。

### 1. 「枚」を数詞ではなく名詞として使っていた箇所

「そのスライド」「すべてのスライド」に言い換えた。`1枚`・`3枚目`・`枚数` の
ように数詞として使っている箇所は直していない。

- `docs/User.md` — 見出し「「この枚を見て」と渡す」、「その枚から始まる」
  「同じ枚に戻る」「その枚だけ書き出す」「全枚の読み上げ秒数」
- `docs/Developer.md` — 「その枚から始める」
- `player.html` — `// 見ている枚を URL に載せる`
- `slides/template.js` — 「他の枚には影響しない」

見出しを変えたので、`README.md` と `docs/User.md` のアンカーも
`#2-最初の-1枚を書いて再生する` に合わせた。

### 2. 文法が壊れていた箇所（`docs/Developer.md`）

- 「17 枚は `ffprobe` で測定し、入れた値（合計 324 秒）で、目分量ではない」—
  述語がつながっていなかったので「`ffprobe` で測って入れた値」にした
- 「同じ値なため」「そのものなため」— 名詞に「な + ため」が付いていたので
  「なので」にした（「十分なため」は形容動詞なのでそのまま）
- 「（オフラインでは崩れる）。`public_html/` に置くため、ファイルを置くだけで
  公開される」— 前文と関係の無い話が続き「置く」も重複していたので、
  段落を分けて「このリポジトリは `public_html/` の下にあるので、ファイルを
  置けばそのまま公開される」にした

### 3. 紛らわしい語

- `src/ytslide/measure.py` の「当たるスライド」— 同じファイルの「当てる」は
  置換規則が適用される意味で使っているため、「対象のスライド」にした
  （`docs/Developer.md` の言い方に揃えた）
- `player.html` の「落ち着いた優艶な日本語女性音声を優先検索」—
  「落ち着いた日本語の女性音声を優先して探す」
- `player.html` の「読み上げの解錠」— `docs/Developer.md` の書き方に合わせて
  「読み上げの unlock」

### 4. 表記ゆれ

- 「読み置換表」→「読みの置換表」（`docs/Developer.md`）
- **数字と日本語の間の半角スペースを、詰める方に統一した**（利用者の判断）。
  文書は「1 つ」「19 種」と空け、スライドの表示されるテキストは「1要素が1枚」
  と詰めていて揃っていなかった。詰めたのは数を数える表現だけで、
  比率・バージョン・測定値（`16:9 の`、`Python 3.13 以上`、`324 秒`、
  `180 字`、`1.4 倍`、`1 バイト`）は空けたまま。149 箇所
- `slides/user.js` の題名「player.html で別のスライドを作る」→
  「自分のスライドを作る」（`slidesConfig.title`・`slideData[0].title`・
  `render()` の中の見出しの 3 箇所）
- `slides/user.js` 1 枚目のナレーション「ご紹介します」→「紹介します」。
  他のナレーションは「〜します」で揃っている

`TODO.md` の完了済みの目次と `archives/` は触っていない。目次の見出しは
`archives/todo/` のファイル名と一致させてあるため。

## 確かめたこと

- **ナレーションを変えた 11 枚の `duration` を測り直した**
  （`ytslide measure --slides <名前> <番号> --write`）。`readme` の 1・6、
  `user` の 1・4・5・6・13、`template` の 3・4・10・11。**すべて変更なし**で、
  スペースを詰めても読み上げの長さは変わらないことを実測で確かめた
- `ytslide index` を実行。`index.html` の差分は `template` の説明の 1 行
  （`19 種` → `19種`）だけで、生成結果と手で直した内容が一致している
- `uv run pytest` — 26 件成功
- **verifier がブラウザで表示を確認**（`http.server` + Playwright/chromium、
  1280x800）。`readme` の 1・6、`user` の 1・4・5・6・13、`template` の
  3・4・10・11、`index.html` のスクリーンショットを撮り、文字の欠け・化け、
  枠からのはみ出し、折り返しの崩れが無いことを見た。
  `archives/agents/TODO-111/shots/` にある
- 残っていないことの確認（どちらも出力が空）
  - `grep -rnoP '[0-9] (枚|つ|回|種類|種|カラム|箇所|列|本|行|件|系統|段|個|組|度|グループ|ルール|ファイル)' …`
  - `grep -rn 'その枚\|この枚\|同じ枚\|全枚\|他の枚' …`

## 分担の振り返り

- **verifier が見つけたもの。** `slides/user.js` の題名を 3 箇所のうち
  `render()` の中の 1 箇所だけ直し忘れていたのを、スクリーンショットから
  見つけた。`title` は直っているので `grep 'player.html で別のスライド'` を
  打たない限り気づかない類いで、**画面を見る担当を分けた効果がそのまま出た**。
  `pytest`・`grep`・アンカーの照合は、いずれも食い違い無し
- **見込みとの差。** 見込みどおり main + verifier で足りた。reviewer は
  入れなかったが、分岐も条件式も変えていないので不足は無かった。
  料金は main が 80%。調査と置換を main が全部やったため
- **次に同じ規模（文言だけ・画面に出るテキストを含む）の項目をやるなら。**
  同じく main で直し、verifier に画面確認だけを渡す形でよい。ただし
  **今回のように「同じ語が 2〜3 箇所にある」直しでは、依頼文に
  「`grep '<旧い語>'` が空になることを確かめる」まで書いておく**と、
  スクリーンショットを撮る前に見つかり、撮り直しの 1 往復（$0.3 相当）を
  省ける
