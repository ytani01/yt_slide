# TODO-064. TODO-061 の 1〜13 位を `~/.claude/CLAUDE.md` に足す

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5 / effort high | verifier |
| 実施 | Opus 5 / effort high | verifier |

| 担当 | モデル | effort | output | cache_creation | 料金の割合 |
|------|--------|--------|--------|----------------|-----------|
| main | Opus 5 | high | 11,249 | 118,400 | 83% |
| verifier | Sonnet 5 | medium | 7,160 | 81,801 | 17% |
| 合計 |  |  | 18,409 | 200,201 | 概算 $1.8 |

- verifier は定義（`~/.claude/agents/verifier.md`）のまま sonnet / medium
- 集計は `--since '2026-09-20 04:03:20'` で切った。TODO-064 を立てたコミット
  （`75316db`）のあとに TODO-066 を立てるコミットが挟まっているため

## きっかけ

TODO-061 で、`archives/todo/` 47 件の「分担の振り返り」から共通ルールの候補を
15 件出し、トークン節約に効く順に並べた。候補を出しただけで
`~/.claude/CLAUDE.md` には入れていなかった。

## やったこと

**「トークン節約に効く順」の 1〜13 位を採った。** 14 位（候補 10. 入口や引数が
増えるなら reviewer）と 15 位（候補 9. 実機）は採らない。どちらもトークンが
増える側で、今回の目的から外れる。

- `~/.claude/CLAUDE.md` に **「## サブエージェントへの依頼」** の節を新しく作り、
  12 件を 1〜2 行ずつに圧縮して、効く順に並べた。節の頭に「上ほど効く」ことと
  「後ろの 3 つはトークンが増えるが節約を理由に外さない」ことを書いた
- **候補 4（境界線上の判断は報告だけさせる）は新しい節に書かず**、既存の
  「確認・レビューの担当には、コードを直させない」の箇条書きへ足した。
  既にある記述の補強であって、独立した項目ではないため

### 着手時に決めたこと

- **根拠（TODO 番号）は書かない。** 立てたときは
  「`yt_slide の TODO-028` の形で書く」と決めていたが、着手時に利用者が
  書かない方針に変えた。3 つ目のチェック項目「根拠の番号が合っているか」も
  そのぶん確認から外した
- **チェック項目の「4 と 10 は既にある記述の補強」は、本文の「候補 10 は
  採らない」と食い違っていた。** 利用者に聞き、本文どおり候補 10 は採らず、
  補強として元の箇条書きへ足すのは候補 4 だけにした

## 確かめたこと

verifier に、TODO-061 の表と追記後の `~/.claude/CLAUDE.md` を突き合わせさせた。
報告は `archives/agents/TODO-064/verifier-report.md`。

- 1〜13 位の 13 件すべてに対応する記述がある（順位 → 候補番号 → 場所の対応表）
- 採らないと決めた候補 10・候補 9 は混入していない
- 新しい節の並びは「トークン節約に効く順」のとおり
- 候補 1「測った値が載っていなければ測っていない」、候補 2「計測スクリプトを
  残す」、候補 6「verifier は拾えない」の要点は落ちていない

verifier からは「元の候補にあった個別の具体例（数値の実例）が要約で省かれて
いる」という指摘があった。**1〜2 行に圧縮するのが元々の方針なので、そのまま
とした。**

## 分担の振り返り

- **verifier は、13 件の対応表と「採らない 2 件が混入していないか」を
  `grep` で確かめて返した。** 見落としは無かった。一方で、指摘として
  上がったのは「具体例が省かれている」だけで、これは方針どおりのもの。
  **文書へ書き写す作業では、確認の担当が見つけるものは多くない**
- 見込み（verifier のみ）と食い違わなかった。分岐は変わらないので
  reviewer は入れていない
- **1 巡目の verifier は Sonnet のレート制限で途中終了した。**
  立て直さず `SendMessage` で同じ担当を再開させ、そのまま完了した。
  立て直していれば依頼書の読み直しから始まっていた
- 次に同じ規模（文書だけを変え、素材との突き合わせが十数件）をやるなら、
  同じく verifier 1 人でよい。突き合わせの数が 10 を超えるので、
  main が自分で確かめる形にはしない
