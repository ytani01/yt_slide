# TODO-086 Gemini 方針確認

確認日: 2026-09-21

- `git diff -- TODO.md` で、既存スライドを入力、約30秒、複数要点と短い読み上げ、生成ごとの AI 自動要約、Gemini API が背景に反映されていることを確認した。
- 「手書きの portrait データ」「60秒以内」は「従来案」として経緯にのみ残り、実装案・検証の現行要件にはなっていない。縦型・字幕付きは継承されている。
- 現状確認（既存 CLI の横長 MP4/SRT、原稿抽出、未検証の Gemini API）と実装案（summary CLI、JSON Schema、縦型テンプレート、再生成）が概ね区別され、実装未着手も明記されている。
- 公式資料 URL は Gemini 構造化出力/API/API キーと FFmpeg 字幕フィルターを参照している。実 API 未検証、キー未設定時、固定応答による JSON/失敗系の検証方針も記載されている。
- 残る検証不足は、約30秒の合格許容範囲が未決定、要約の「根拠なし追加・意味反転」の判定基準が未定量、API キーがログ・JSON・生成物・ブラウザへ漏れないことの自動確認が明記されていない点。いずれも試作・実装時に補う必要がある。

## 追加確認

- ダミー API キーを正常時・例外時のログと生成物で検査し、値を出さないことが追記された。要約についても、参照番号との照合、数値・固有名詞・否定・条件の保持、2点以上の要点、人が事前に挙げた必須要点との比較が追記された。
- 約30秒の許容範囲は、試作の自然さを見て利用者に確認する未決事項として既に明記されている。前回の不足指摘は解消した。
