# TODO-101 の分担

コードは変えないが、README に書いたとおりに試せるコマンド手順なので、
確認を main から分けた（TODO-017）。挙動の変更が無いので reviewer は
立てていない。

| 担当     | モデル   | effort | 報告                 |
|----------|----------|--------|----------------------|
| verifier | Sonnet 5 | medium | `verifier-report.md` |

- 依頼文には、**利用者の既存の `ytslide` を壊さないこと**（`UV_TOOL_DIR` と
  `UV_TOOL_BIN_DIR` を一時ディレクトリへ向ける）を明記した
- 1 回目で「README どおりのコマンドが失敗する」ことが分かり、原因が
  GitHub の既定ブランチだと判明した。管理者が既定ブランチを直したあと、
  **担当を立て直さず同じ verifier に続けて頼んだ**。報告も同じファイルへ
  追記させた

振り返りは
`archives/todo/TODO-101. cmd を main へマージして、README の手順で入ることを確かめる.md`
の「分担の振り返り」にある。
