# TODO-091 の分担

実装は main が行った（`player.html` 1 ファイル + 文書 2 ファイルで収まる規模）。

| 担当 | 役割 |
|------|------|
| reviewer | 差分を規約と設計に照らして見る。挙動が変わる項目なので入れる |
| verifier | プルダウンの組み立て・保存・復元・既定への戻りを実測で確かめる |

reviewer を先、verifier を後に回す（reviewer の指摘で実装が変わると、
verifier の実測が古い差分に対するものになるため）。

実際の声の違いは自動では確かめられない（ヘッドレスの Chromium に音声合成
エンジンが無い。TODO-088 で実測）。耳での確認は利用者が実機で行う。
