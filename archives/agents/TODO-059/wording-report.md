# TODO-059 文言書き換え — 報告

「HTML 1 枚で動く」を「ファイル 3 つを置くだけで動く」に統一しました。

## 変更箇所

### 1. README.md:3（冒頭の一文）
- **変更前**: `**HTML 1 枚で、ナレーション付きのプレゼンが動く。**`
- **変更後**: `**ファイル 3 つを置くだけで、ナレーション付きのプレゼンが動く。**`

### 2. slides/readme.js:5（ブラウザタブ名）
- **変更前**: `title: 'yt_slide - HTML1枚で動くナレーション付きプレゼン',`
- **変更後**: `title: 'yt_slide - ファイル 3 つで動くナレーション付きプレゼン',`

### 3. slides/readme.js:23（スライド 1 のナレーション）
- **変更前**: `'yt_slide は、HTML1枚でナレーション付きのプレゼンが動き出す仕組みです。ビルドもインストールも不要で、URLを渡すだけでそのまま見てもらえます。'`（65 文字）
- **変更後**: `'yt_slide は、3 つのファイルを置くだけで、ナレーション付きのプレゼンが動き出す仕組みです。ビルドもインストールも不要で、URLを渡すだけでそのまま見てもらえます。'`（71 文字）

### 4. slides/readme.js:31-33（スライド 1 の h1 見出し）
- **変更前**:
  ```
  HTML <span class="text-transparent bg-clip-text bg-gradient-to-r from-sky-400 to-lime-400">1枚</span> で、
  ナレーション付きのプレゼンが動き出す
  ```
- **変更後**:
  ```
  ファイル <span class="text-transparent bg-clip-text bg-gradient-to-r from-sky-400 to-lime-400">3 つ</span> を置くだけで、
  ナレーション付きのプレゼンが動き出す
  ```
- グラデーション強調の span は保持し、テキストのみを差し替え

### 5. slides/readme.js:260（まとめスライドのナレーション）
- **変更前**: `'まとめです。yt_slide を使えば、HTML 1枚でナレーション付きのプレゼンをすぐに作って公開できます。ぜひ試してみてください。'`（54 文字）
- **変更後**: `'まとめです。yt_slide を使えば、ファイル 3 つを置くだけで、ナレーション付きのプレゼンをすぐに作って公開できます。ぜひ試してみてください。'`（61 文字）

### 6. slides/readme.js:270（まとめスライドの強調テキスト）
- **変更前**: `<strong>HTML 1枚</strong> でナレーション付きプレゼンが動く`
- **変更後**: `<strong>ファイル 3 つ</strong> を置くだけで、ナレーション付きプレゼンが動く`

## 確認

grep で確認済み：

```bash
grep -rn "HTML *1 *枚\|HTML1枚" --include="*.md" --include="*.js" . | grep -v "^./archives/"
```

結果：TODO.md（項目の説明）以外に該当なし。archives/ ファイルは変更対象外のため保持。

## 保持した内容

- README.md の他の節や docs/ 配下は変更なし
- HTML 構造・クラス・style 属性は保持
- 他のスライドファイルは変更なし
- ナレーション長は元の長さに近い範囲内（測り直し最小化）
