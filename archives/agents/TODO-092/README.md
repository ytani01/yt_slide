# TODO-092 の分担

実装は main が行った（`player.html` の markup の移動だけ。消音のロジックは触っていない）。

| 担当 | 役割 |
|------|------|
| verifier | 移設先の位置、切り替え、`M` キー、消音中の自動送り、残骸の有無を実測 |

reviewer は入れていない。分岐も条件式も変えておらず、ID を据え置いたまま
markup を移しただけのため。

TODO-091 の verifier をそのまま続けて使った（環境と計測スクリプトの
作り方を組み直さずに済むため）。
