# スライド実装の報告

実装担当 slides099（gpt-5.6-terra / medium）の通知を main が記録。
実装担当には archives を変更できない制約があったため代筆した。

- 手順1〜4を3枚（準備／書いて確かめる／一覧・仕上げ・共有）へ統合。
- 最初は User.md の最小例を sample.js に保存し、型のコピーは任意にした。
- `index` と任意の `update`、共有を後段に整理。
- 後続の duration 説明、テンプレート19種、総枚数を更新。
- 担当の確認: `node --check`、slideData の読み込み、全 narration 180字以内、
  `git diff --check` は通過。
- main が変更前と照合: 16枚。ナレーション変更は3・4・5・11・12枚目。
  測り直しとブラウザ確認は別工程で行う。
