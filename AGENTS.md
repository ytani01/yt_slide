# AGENTS.md

このリポジトリで作業する Codex 向けの指示。

## 共通の指示

作業前に、リポジトリ直下の `CLAUDE.md` を最後まで読み、このリポジトリの
構成、変更前に読む文書、文書の管理方針を Codex でも適用する。
共通の説明は複製せず、`CLAUDE.md` とそこから参照する `docs/` を正本とする。
利用者の直接の指示を優先する。

## Claude Code との分離

- `CLAUDE.md`、`.claude/`、`~/.claude/` は、利用者が明示しない限り変更しない。
- Codex 固有の指示はこの `AGENTS.md`、リポジトリ固有の設定が必要になった
  場合は `.codex/` で管理する。
- モデル、reasoning effort、サブエージェントは Codex 側の設定を使う。
  Claude 固有の機能や料金・トークン集計は流用しない。
- ソースコードを変更する場合は、既存の `TODO.md` の番号と運用を引き継ぐ。
  過去の担当・モデルの記録を Codex のものに書き換えない。

## 検証

変更に応じて `tests/` のテストを `uv run pytest` で確認する。プレイヤーや
スライドの表示・操作の変更は、対象の `player.html?slides=<名前>` を
ブラウザで開いて確かめる。
文書や指示ファイルだけの変更では、参照先と指示の整合性を確認する。
